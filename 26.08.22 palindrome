#include<stdio.h>
#include<string.h>
void main()
{
	int i,j,size;
	char s[20];
	printf("enter any string\n");
	gets(s);
	size=strlen(s);
	i=0;j=size-1;
	while(i<(size/2))
	{
		if(s[i]=s[j])
		{
		printf("palindrome");
		}
		else{
		printf("Not palindrome");
		}
		i++;
		j--;
	}
}
